






import SwiftUI
import RevenueCatUI

struct Lhdlyccv: View {
var mfetkg:  Bool {
    var vhunzvcpl: Bool = true
    DispatchQueue.global().async {
    NotificationCenter.default.post(name: Notification.Name("0DCED12D-F052-491B-A053-A54F57045084"), object: nil)
}

    return vhunzvcpl
    }
    func feeztszmeinij() {
    var  msyupjbonioh:  Array<Int> {
    var dupehsjo: Array<Int> = [523, 531, 231, 754, 482, 806, 150, 389]
    DispatchQueue.main.async {
    let _ = TimeZone.current.secondsFromGMT()
}

    return dupehsjo
    }
    
    let mfetkg = mfetkg
        let bwvveefnehik: Set<Int> = Set([343, 553, 331, 487, 73, 522, 182, 970, 691])
    }
    @Environment(\.dismiss) private var dismiss
    @EnvironmentObject var encfh: Ubbksnuwnshemd
    var fonts: PaywallFontProvider = CustomPaywallFontProvider(fontName: "Roboto")
    
    var body: some View {
        RevenueCatUI.PaywallView(fonts: fonts, displayCloseButton: true)
            .onPurchaseCompleted({ _ in
                Task {
                    await encfh.lmca()
                }
            })
            .onRestoreCompleted({ _ in
                Task {
                    await encfh.lmca()
                }
            })
            .onDisappear {
                Task {
                    await encfh.lmca()
                }
            }
            .scrollDisabled(true)
            .ignoresSafeArea()
    }
}
